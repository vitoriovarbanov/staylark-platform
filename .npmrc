# Hoist type definitions and linting tools for IDE and ESLint flat config resolution
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=prettier

# Run security audit on install
audit=true

# Suppress funding messages
fund=false