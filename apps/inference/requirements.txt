# CPU-only torch: Railway has no GPU, so pull the CPU wheel from PyTorch's index
# (strips the ~2-3 GB bundled CUDA libraries — smaller image, faster cold start,
# and fewer vendored libs for the scanner to flag). The `+cpu` local tag forces
# the CPU build deterministically; a bare `torch==2.4.*` here could still resolve
# the CUDA wheel from PyPI. Linux-only build — local dev uses the lightweight venv.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
faster-whisper==1.0.*
transformers==4.44.*
torch==2.4.1+cpu
python-multipart==0.0.*
pytest==8.*
httpx==0.27.*
